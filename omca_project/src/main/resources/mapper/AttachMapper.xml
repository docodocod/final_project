<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.omca.dao.AttachDao">
	<select id="getAttachList" resultType="com.project.omca.bean.AttachImage">

		select * from product_image where p_id = #{p_id}

	</select>

</mapper>